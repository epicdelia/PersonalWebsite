<div class="wrapper wrapper_padding clearfix">
	<link rel="stylesheet" href="css/welcome.css" />
	<div class="col-1 about homepage_content">
		<h2>About</h2>
		<hr>
		<p>I just graduated from the Business and Computer Science Double Degree Co-op program at <a href="https://www.wlu.ca/" target="blank"> Wilfrid Laurier University</a> <p>
		<p>I am passionate about using innovative technologies to solve business problems.<p>
	</div>
	<div class="col-2 homepage_content">
		<h2>Stuff I've written</h2>
		<hr>
		<% @posts.each do |post| %>
		<h3><%= link_to post.title, post_path(post) %></h3> 
		<p class="date"> <%= post.created_at.strftime("%A, %b, %d") %> </p>
		<% end %>
	</div>
	<div class="col-3 homepage_content">
		<h2>Stuff I've built</h2>
		<hr>
		<% @projects.each do |project| %>
		<h3><%= link_to project.title, project_path(project) %></h3> 
		<p class="date"> <%= project.created_at.strftime("%A, %b, %d") %> </p>
		<% end %>
	</div>
</div>